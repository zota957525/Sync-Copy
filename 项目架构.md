# Sync Copy — 项目架构文档（v2.0）

> 本文档是 v2 重写后的开发者参考架构。所有决策有对应 ADR 可追溯。
> 如需了解产品需求，参见 `specs/` 目录；如需了解技术决策，参见 `decisions/` 目录。

---

## 1. 产品定位

**Sync Copy** 是一个运行在局域网内的多设备剪切板/文件同步桌面工具（`specs/00-product-overview.md` 第 1 节）。

- 形态：Tauri 2 桌面应用，macOS + Windows 双平台
- 核心价值：同一 LAN 下，复制即同步，无服务器、端到端加密、人工审批入组
- 目标用户：单人多机场景（家里 + 办公室，2-5 台设备）
- 同步对象：纯文本（≤1 MB）、PNG 图片（≤5 MB）、文件（≤5 MB）
- 明确不支持：mDNS 自动发现、跨互联网、Linux 一线支持、移动端

v2 是完整重写，v0 prototype 保留在 `legacy-prototype` 分支（`specs/00-product-overview.md` 第 5 节，`decisions/ADR-001`）。

---

## 2. 核心功能

### 2.1 剪切板同步（clipboard-text-sync / clipboard-image-sync）

- 每秒轮询系统剪切板（arboard），内容变化即广播给所有已批准的 peer
- SHA-256 内容哈希防环路（避免从远端写入的内容再次广播）
- 5 秒去重窗口防高频重复广播
- 文本上限 1 MB，图片格式仅 PNG

参见 `specs/clipboard-text-sync.md`、`specs/clipboard-image-sync.md`

### 2.2 文件传输（file-transfer-drag）

- 用户拖文件到浮窗触发发送，接收端弹确认框
- 单文件上限 5 MB（协议层 413 拒绝）
- 保存到接收端系统 Downloads 目录，同名自动加后缀

参见 `specs/file-transfer-drag.md`

### 2.3 小组管理与 Gossip Mesh（group-discovery / group-trust-gossip）

- 手动输入对方 IP:PORT 发起握手（无 mDNS 自动发现）
- 握手响应携带已知 peers 列表，客户端自动 gossip dial 扩展为完整 mesh
- `GOSSIP_MAX_CONCURRENT=3` 限制并发握手防 cascade 风暴
- 分布式审批：任一在线设备点同意即全组生效，其他弹框自动 dismiss

参见 `specs/group-discovery.md`、`specs/group-approval.md`

### 2.4 心跳与隐形掉线检测（peer-heartbeat）

- 5 秒间隔主动 ping，连续 5 次失败（≈25-30 秒）触发强制重连
- 广播失败计数独立于心跳：连续 2 次广播失败触发被动健康自检
- 强制重连 6 步（Shutting/banned 校验 → ClientPool.replace → 窗口期重检 → dial_handshake → reset 计数）
- `last_successful_sync_at` 仅在广播 200 OK 时更新（区别于 ping 成功时间）

参见 `specs/peer-heartbeat.md`、`decisions/ADR-010` 第 3.6 节

### 2.5 历史记录（history-list）

- 内存中保存最近 50 条同步内容（VecDeque + FIFO）
- 支持单条删除、清空全部，v2 阶段跨机同步删除
- 重启后历史清空（有意设计，不落盘）

参见 `specs/history-list.md`

### 2.6 设置（settings-panel）

- 设备名编辑（持久化到 ProjectDirs JSON）
- 清除历史、退出应用（退出前广播 leave）
- 端口 P1 阶段不开放 UI 修改

参见 `specs/settings-panel.md`

---

## 3. 技术栈

### 3.1 后端（src-tauri/）

| 组件 | 版本 | 用途 |
|---|---|---|
| Tauri | 2.x | 桌面应用框架（features: macos-private-api, tray-icon） |
| tokio | full features | 异步运行时（HTTP server + health worker） |
| axum | 0.8 | HTTP server（LAN 端点） |
| reqwest | 0.12 | HTTP client（广播 + 握手，no_proxy） |
| arboard | 3 | 系统剪切板读写 |
| image | 0.25 | PNG 编解码（仅 PNG feature） |
| x25519-dalek | 2 | ECDH 临时密钥对 |
| hkdf | 0.12 + sha2 | HKDF-SHA256 密钥派生 |
| aes-gcm | 0.10 | AES-256-GCM 加密 |
| zeroize | 1.8 | 密钥内存清零（`Zeroizing<[u8;32]>`） |
| parking_lot | | RwLock / Mutex（PeerRegistry 三把锁） |
| if-addrs | 0.13 | LAN IP 枚举与优先级筛选 |
| tracing + tracing-subscriber | | 结构化日志 |
| thiserror + anyhow | | 错误处理 |
| uuid + rand | | device_id 生成 |
| serde + serde_json | | DTO 序列化 |
| base64 | 0.22 | nonce/ciphertext 编码 |
| directories | | ProjectDirs（配置文件路径） |

### 3.2 前端（src/）

| 组件 | 版本 | 用途 |
|---|---|---|
| SvelteKit | 2 | 应用框架（adapter-static, ssr=false） |
| Svelte | 5 | UI 组件（runes: $state / $derived / $effect） |
| TypeScript | 5.6+ | 类型安全 |
| Vite | 6 | 构建工具 |
| @tauri-apps/api | 2.x | Tauri IPC（invoke / listen） |
| @tauri-apps/plugin-opener | 2.x | Finder/Explorer 文件定位 |

---

## 4. 系统架构

### 4.1 模块切分（决策：ADR-003 第 3.1 节 选项 B）

```
src-tauri/src/
├── main.rs                      # Tauri 启动器，仅 init + run
├── lib.rs                       # 库入口；install_panic_hook + register commands + start runtimes
├── app/
│   ├── state.rs                 # AppState 顶层聚合（持有子域 Arc）
│   └── lifecycle.rs             # Lifecycle struct（4 Phase：Booting/Running/Shutting/Dead）
├── config/
│   └── persistence.rs           # Config 读写（ProjectDirs JSON）
├── crypto/
│   ├── mod.rs                   # KeyExchange + Sealer trait + build_aad + AadKind enum
│   ├── x25519.rs                # X25519KeyExchange impl（HKDF_SALT/INFO 常量唯一定义点）
│   └── aes_gcm.rs               # AesGcmSealer impl
├── clipboard/
│   ├── mod.rs                   # spawn 入口（std::thread）
│   ├── poll.rs                  # 1s 轮询循环（image 优先 / text 兜底）
│   ├── encode.rs                # PNG 编解码 + last_text/last_image_hash 环路防护
│   └── cmd.rs                   # ClipboardCmd enum + mpsc 通道
├── history/
│   ├── mod.rs                   # HistoryStore（VecDeque + RwLock + MAX_HISTORY=50）
│   └── dedup.rs                 # content_hash 去重
├── peer/
│   ├── mod.rs                   # PeerRegistry 实现（13 个公开方法）
│   ├── state.rs                 # PeerState struct + TrustState enum
│   └── sanitize.rs              # sanitize_device_name（Bidi/控制字符黑名单 + 64 codepoints）
├── network/
│   ├── mod.rs
│   ├── protocol.rs              # 所有 DTO（HandshakeReq/Resp / ClipboardReq / FileReq 等）
│   ├── server.rs                # axum router + 共享 middleware
│   ├── handlers/
│   │   ├── handshake.rs         # POST /handshake（ECDH + 审批流程）
│   │   ├── clipboard.rs         # POST /clipboard（text + image_png）
│   │   ├── file.rs              # POST /file
│   │   ├── approval.rs          # POST /peers/approval/{forward,decide,dismiss}
│   │   ├── gossip.rs            # POST /peers/{trust,ban,leave,announce}
│   │   ├── history.rs           # POST /delete_history + /history/clear
│   │   └── ping.rs              # GET /ping
│   ├── client.rs                # broadcast_clipboard / broadcast_leave / dial_handshake
│   ├── client_pool.rs           # ClientPool（per-peer reqwest::Client，禁 lazy add）
│   ├── health.rs                # 心跳 worker + 隐形掉线健康自检 + force_rebuild 6 步
│   ├── rate_limit.rs            # RateLimiter（handshake DoS 限流，独立于 PeerRegistry）
│   └── lan_ip.rs                # if-addrs 枚举 + 优先级筛选（192.168 > 10 > 172.16）
└── commands/
    └── mod.rs                   # 11 个 Tauri 命令（quit_app / get_status 等）
```

前端组件树（`src/lib/components/`）：

- `FloatingWindow.svelte` — 主容器（320×420，透明置顶）
- `FloatingBall.svelte` — 收缩球形态（48×48，双击展开）
- `FloatingHeader.svelte` — 顶部状态栏（StatusDot + 加入按钮 + 设置按钮）
- `StatusDot.svelte` — 绿/灰/黄/橙状态指示点
- `HistoryList.svelte` — 历史列表主体（滚动容器 + 空态文案）
- `ApprovalDialog.svelte` — 审批弹框（握手审批 + 文件接收复用）
- `SettingsPanel.svelte` — 设置面板（设备名 + 清除 + 退出）
- `ClearConfirm.svelte` — 清除历史二次确认
- `FloatingBall.svelte` — 悬浮球

参见 `decisions/ADR-003` 第 3.1 节

### 4.2 AppState 结构

```rust
// app/state.rs
pub struct AppState {
    pub peers: Arc<PeerRegistry>,
    pub rate_limiter: Arc<RateLimiter>,
    pub history: Arc<HistoryStore>,
    pub config: Arc<RwLock<Config>>,
    pub clipboard_apply_tx: mpsc::Sender<ClipboardCmd>,
    // 通信句柄（lifecycle 填充）
}
```

### 4.3 Lifecycle 4 阶段（ADR-010）

| Phase | 含义 | 持续时间 |
|---|---|---|
| Booting | `start()` 7 步进行中 | 数百毫秒 |
| Running | 正常服务期 | 用户使用期间 |
| Shutting | `shutdown()` 7 步进行中 | ≤ 2800ms |
| Dead | 进程即将 exit | 瞬时 |

启动 7 步顺序：tracing init → Config load → 实例化 Arc<ClientPool/PeerRegistry/RateLimiter/History> → clipboard::spawn（std::thread）→ axum bind → health::spawn → emit app-ready。

关闭 7 步顺序：phase=Shutting → 发 cancel + ClipboardCmd::Shutdown → leave 广播（1500ms 超时）→ join worker + thread（500ms + 100ms）→ server graceful shutdown（500ms）→ peers.clear() → drop log_guard + app.exit(0)。

参见 `decisions/ADR-010`

---

## 5. 网络协议

### 5.1 HTTP 端点完整表（ADR-003 第 3.2 节）

| 路径 | 方法 | 功能 | 加密 |
|---|---|---|---|
| `/handshake` | POST | X25519 公钥交换 + 审批触发 | 明文（公钥本身无机密性） |
| `/clipboard` | POST | 剪切板内容广播（text/image_png/snapshot） | AES-256-GCM |
| `/file` | POST | 文件传输 | AES-256-GCM |
| `/peers/approval/forward` | POST | 审批请求转发（gossip 给其他 peer） | 明文 |
| `/peers/approval/decide` | POST | 审批结果回流 | 明文 |
| `/peers/approval/dismiss` | POST | 审批弹框撤销 | 明文 |
| `/peers/trust` | POST | trust gossip 传播 | 明文 |
| `/peers/ban` | POST | ban gossip 传播 | 明文 |
| `/peers/leave` | POST | 主动下线广播 | 明文 |
| `/peers/announce` | POST | 新成员加入后广播给已知 peer（gossip mesh） | 明文 |
| `/delete_history` | POST | 跨机同步删除历史条目 | 明文 |
| `/history/clear` | POST | 跨机清空历史 | 明文 |
| `/ping` | GET | 心跳探活 | 明文（无机密信息） |

### 5.2 通用 HTTP Header

每个请求携带：

- `X-SC-Device-Id`：origin device_id（快速鉴权 fast path）
- `X-SC-Seq`：单调递增序列号（dedupe lookup 前置检查）
- `X-SC-Auth`：占位（v2 未实现，留 PSK 扩展位）

body 字段仍为权威，header 用于 middleware fast-fail。

### 5.3 状态码语义（ADR-003 第 3.2 节 + ADR-008 第 4.1 节）

| 码 | 语义 |
|---|---|
| 200 | 成功（含 seq dedupe 命中的静默 200） |
| 400 | 请求格式错 / size 字段不通过 |
| 403 | 鉴权失败（统一 body，不区分"banned"/"未知"/"拒绝"三种内部路径） |
| 408 | 审批超时（30s） |
| 409 | device_id 冲突（v2 实际映射为 403，避免 device_id 枚举攻击） |
| 413 | 请求体过大（>5 MB） |
| 422 | 解密失败 / 尺寸 mismatch（统一 body，不区分具体原因） |
| 429 | RateLimiter 触发限流（handshake DoS 防御） |
| 500 | 写盘失败 / 不可恢复内部错 |

### 5.4 主要 DTO

```
HandshakeReq  { device_id, device_name, listen_port, pubkey_b64 }
HandshakeResp { device_id, device_name, pubkey_b64, peers: Vec<PeerStub> }
PeerStub      { device_id, addr }                        # gossip 扩展用最小结构

ClipboardReq  { origin_device_id, seq, kind, nonce_b64, ciphertext_b64 }
              kind: "text" | "image_png" | "snapshot"
FileReq       { origin_device_id, seq, filename, size, nonce_b64, ciphertext_b64 }
```

---

## 6. 安全模型

### 6.1 密码学栈（ADR-003 第 3.4 节 + ADR-008 + ADR-011）

| 组件 | 选型 | 来源 |
|---|---|---|
| 密钥协商 | X25519 ECDH（EphemeralSecret，每次握手新密钥对） | x25519-dalek 2.x |
| KDF | HKDF-SHA256 | hkdf 0.12 + sha2 |
| HKDF salt | `b"sync-copy-v2-salt"` | crypto/x25519.rs 顶部 const（唯一定义点） |
| HKDF info | `b"sync-copy-v2:aes-256-gcm"` | 同上 |
| AEAD | AES-256-GCM（96-bit nonce + 128-bit tag） | aes-gcm 0.10 |
| nonce 来源 | `rand::rngs::OsRng`（12B CSPRNG） | 由 Sealer impl 内部生成，调用方不可注入 |
| 内存清零 | `Zeroizing<[u8;32]>` 包 PeerState.aes_key | zeroize 1.8 |

每对 peer 独立一把 AES-256 密钥（每会话），密钥仅内存，进程退出即丢（前向保密）。

### 6.2 AAD 绑值（ADR-008 第 3.6 节 + ADR-011 第 3.3 节）

所有加密报文的 AAD = `b"sync-copy-v2" || kind.as_bytes() || origin_device_id.as_bytes() || seq.to_be_bytes()`

由 `crypto::build_aad(kind: AadKind, origin_device_id, seq)` 集中构造，禁止 handler 散点拼装。

防御目标：
- 跨 kind 重放（text 报文伪装为 image_png）
- 跨 origin 重放（C 截获 A→B 报文后改 origin 重发给 B）
- seq 层密码学级去重（在应用层 `seen_seq_and_update` 之外的第二道闸）

### 6.3 8 必修安全条款（ADR-008 第 7.2 节，全部在 v2.0 代码层闭环）

| 编号 | 内容 | 落地位置 |
|---|---|---|
| MUST-1 | AAD 绑值（kind+origin+seq 三维） | crypto/aes_gcm.rs + build_aad |
| MUST-2 | aes_key 用 Zeroizing 包装，drop 自动清零 | peer/state.rs::PeerState.aes_key |
| MUST-3 | 403 通用 body（3 种 forbidden 路径不可区分） | network/handlers/handshake.rs |
| MUST-4 | PeerRegistry.remove 内嵌 client_pool.remove，顺序原子 | peer/mod.rs::PeerRegistry::remove |
| MUST-5 | panic message 不含运行时变量插值 | 全局约定，code-reviewer PR 阶段 grep |
| MUST-6 | /file 端点加 seq dedupe（v0 漏洞修复） | network/handlers/file.rs |
| MUST-7 | handshake DoS 限流（per-pair 60s ≤ 3，全局 60s ≤ 10） | network/rate_limit.rs::RateLimiter |
| MUST-8 | device_name 三函数 sanitize（Bidi+控制字符+64 codepoints） | peer/sanitize.rs + commands.rs::set_config |

### 6.4 PeerRegistry 不变式（ADR-009）

1. `approved ∩ banned = ∅`（互斥覆盖，永不同时在两集合）
2. `inner[id].trust_state == Approved ⟺ approved.contains(id)`
3. `client_pool.contains(id) == inner.contains_key(id)`（remove 原子顺序保证）

锁顺序固定：`inner > approved > banned`（防 AB-BA 死锁，dev profile 用 parking_lot deadlock detector 检测）。

---

## 7. UI/UX 规格

### 7.1 浮窗（floating-window）

- 逻辑尺寸：320×420，`resizable: true`（框架约束）
- `alwaysOnTop: true`，`decorations: false`，`transparent: true`
- 圆角 10px，`backdrop-filter: blur(20px)`（macOS 需 `macOSPrivateApi: true`）
- 启动时居中（`center: true`），每次 show 调 `ensure_on_screen`

### 7.2 悬浮球（floating-ball）

- 收缩为 48×48 圆形，border-radius: 50%
- 手势消歧：移动 ≤8px 视为点击（展开），移动 >8px 视为拖动
- `lastExpandedSize` 记录展开前尺寸，展开后恢复

### 7.3 系统托盘（tray-integration）

- id `main-tray`，tooltip `Sync Copy`
- 左键：切换浮窗显示/隐藏
- 右键菜单：`显示浮窗` / `隐藏浮窗` / `退出`（退出走 quit_app 命令，P0 阶段 app.exit(0) 过渡）

### 7.4 11 个 Tauri 命令

| 命令名 | 功能 |
|---|---|
| `quit_app` | 广播 leave → 关闭（4 退出路径统一入口） |
| `get_status` | 获取连接状态快照（peers 数、phase、本机 IP） |
| `get_peers` | 获取当前 peer 列表 |
| `join_group` | 发起握手（normalize_addr + dial_handshake） |
| `get_config` | 获取配置 |
| `set_config` | 保存配置（device_name 经 sanitize_device_name 三件套） |
| `approve_peer` | 用户点同意审批 |
| `reject_peer` | 用户点拒绝审批 |
| `get_history` | 获取历史列表（Vec<HistoryItem>） |
| `delete_history_item` | 删除单条历史 |
| `clear_history` | 清空历史 |
| `recopy_history_item` | 把历史条目写回剪切板 |

### 7.5 3 个 Tauri 事件

| 事件名 | 触发时机 |
|---|---|
| `status-updated` | peer 列表变化、连接状态变化 |
| `history-updated` | 历史列表新增/删除/清空 |
| `peer-pending` | 新设备请求入组（触发前端弹审批框） |

---

## 8. 目录结构

```
sync-copy/
├── CLAUDE.md                 # 项目宪法（主窗口工作契约）
├── PLAN.md                   # 任务看板（状态机驱动）
├── TEAM.md                   # 虚拟团队花名册
├── README.md                 # GitHub 仓库门面
├── 项目架构.md               # 本文件（开发者视角）
├── 使用说明.md               # 用户视角使用文档
├── CHANGELOG.md              # 版本变更记录
├── package.json              # 前端依赖（version 字段）
├── src-tauri/
│   ├── Cargo.toml            # Rust 依赖
│   ├── tauri.conf.json       # Tauri 配置（窗口/托盘/能力）
│   ├── capabilities/         # Tauri 能力声明
│   └── src/                  # Rust 业务源码（见 4.1 节）
├── src/                      # 前端源码（见 4.1 节）
├── static/                   # 图标等静态资源
├── specs/                    # 产品需求 spec（每 feature 一份）
├── decisions/                # 技术决策 ADR
├── tests/                    # 集成测试脚本 + 手测 checklist
├── docs/
│   └── handoff-lessons-learned.md  # 经验教训记录
└── .claude/
    ├── agents/               # 10 个虚拟同事 prompt
    ├── commands/             # slash 命令
    ├── hooks/                # 安全拦截钩子
    └── settings.json         # SubagentStop + PreToolUse 配置
```

---

## 9. 构建与发布

### 9.1 本地开发

```bash
npm run tauri dev    # 前端 + 后端热重载开发模式
npm run tauri build  # 本地构建产物
```

### 9.2 CI（GitHub Actions）

工作流文件：`.github/workflows/`

| 任务 | 环境 | 产物 |
|---|---|---|
| macOS universal | `macos-latest` | `SyncCopy-v<版本>-macOS-universal.dmg` |
| Windows x64 | `windows-latest` | `SyncCopy-v<版本>-windows-x64-setup.exe` |
| Rust 单测 | ubuntu-latest | cargo test（153 条）|

### 9.3 版本管理

- `package.json` 的 `version` 字段是版本号权威来源
- Git tag 格式：`v<major>.<minor>.<patch>`
- Conventional Commits 格式：`feat:` / `fix:` / `docs:` / `refactor:` / `chore:` / `test:`

---

## 10. 重建提示

新进入项目的开发者或 Claude 会话，按以下顺序阅读即可理解全部架构决策：

1. `specs/00-product-overview.md` — 产品定位、用户故事、项目级验收标准、v0 教训
2. `specs/_assumptions.md` — 事实假设清单（用户校对版，44 条）
3. `decisions/ADR-001` — 重写决策与 SDLC 流程
4. `decisions/ADR-003` — 项目层骨架（模块切分 / 协议 / 数据模型 / 加密 / lifecycle）
5. `decisions/ADR-008` — 安全审阅（8 必修、威胁模型）
6. `decisions/ADR-009` — PeerRegistry 接口契约
7. `decisions/ADR-010` — Lifecycle 启停步序
8. `decisions/ADR-011` — Crypto trait 拆分 + AAD 绑值实现
9. 对应 feature 的 `specs/<slug>.md`（按需查阅）

规则：任何"为什么这样实现"的问题，答案必须在某份 ADR 里找到。找不到意味着需要补 ADR。

---

## 11. 已知限制

| 限制 | 原因 | 参考 |
|---|---|---|
| 不支持 mDNS 自动发现 | 保持协议面积小、无需额外系统权限 | `specs/00-product-overview.md` 第 3 节 |
| 信任关系不持久化 | 安全设计选择：重启即重置，避免陈旧信任 | `specs/_assumptions.md` A22 |
| Linux 不在官方支持 | Wayland 图片剪切板不稳定，CI 不构建 | `specs/00-product-overview.md` 第 3 节 |
| 文本上限 1 MB / 文件上限 5 MB | 防 DoS（内存暴涨），LAN 场景够用 | `specs/_assumptions.md` A13/A16 |
| v2 与 v0 协议不互通 | HKDF v2 salt bump（设计选择，非 bug） | `decisions/ADR-011` 第 3.4 节 |
| N ≤ 8 设备设计目标 | gossip mesh O(N²) 连接数，8 台上限可接受 | `decisions/ADR-003` 第 2.5 节 |
| 剪切板采用 1s 轮询 | arboard 跨平台 API 不提供事件驱动，轮询是当前最稳定方案 | `specs/00-product-overview.md` 第 5.4 节 |
