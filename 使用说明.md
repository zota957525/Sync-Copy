# Sync Copy 使用说明

**Sync Copy — 局域网内多设备剪切板同步桌面工具。同一 LAN 下，一台机器复制，所有受信任设备自动同步。无服务器、端到端加密、人工审批入组。**

---

## 是什么 / 不是什么

**是**：

- 桌上 Mac + Windows 共用同一 WiFi：Mac 上 Cmd+C 一段代码，旁边 Windows 立刻 Ctrl+V
- 家里 Mac 主机 + 客厅笔记本同连路由器：截图后按一下粘贴键，图片直接到另一台
- 拖一个 PDF 到浮窗，对方收到即存到 Downloads（≤5 MB）

**不是**：

- 云剪切板（无中心服务器，离开同一 LAN 就不工作）
- 跨互联网同步（不在同一 WiFi / 有线网段就连不上）
- 跨 VLAN / 跨网段（属于不同子网也不行）
- 文件仓库（单文件 5 MB 上限，不适合传大文件）

---

## 安装

### 下载

从 GitHub Releases 下载对应平台的安装包：

| 平台 | 文件 |
|---|---|
| macOS (Apple Silicon + Intel 通用) | `SyncCopy-v<版本>-macOS-universal.dmg` |
| Windows 10/11 x64 | `SyncCopy-v<版本>-windows-x64-setup.exe` |

### 安装

**macOS**：双击 `.dmg`，把 `Sync Copy.app` 拖进 Applications。如果提示「来自未识别开发者」，右键 → 打开，再确认即可。

**Windows**：双击 `-setup.exe`，跟着向导安装。如果 Windows Defender 警告，选"仍然运行"——这是未签名桌面应用的通病，非恶意程序。

---

## 首次使用（5 步）

### 步骤 1：在所有设备上启动 Sync Copy

- macOS：Launchpad 或 Applications 里双击
- Windows：开始菜单 → Sync Copy

启动后，一个磨砂玻璃风格的浮窗会置顶显示在屏幕上。系统托盘（Mac 菜单栏 / Windows 右下通知区）同时出现 Sync Copy 图标。

### 步骤 2：在 A 设备上查看本机地址

浮窗底部左侧显示本机 `IP:PORT`（默认端口 5858）。点击可复制到剪切板。

```
示例：192.168.1.50:5858
```

### 步骤 3：在 B 设备上输入 A 的地址加入小组

点浮窗顶部蓝色「加入」按钮 → 弹出「加入小组」输入框 → 输入 A 的 `IP:PORT` → 点「加入」。

### 步骤 4：A 设备收到审批弹框，点「同意」

A 上会弹出一个审批框，显示 B 的设备名和 IP。点击「同意」后，双方完成握手。

### 步骤 5：开始使用

A、B 双方浮窗顶部均显示绿色状态点 + `小组 · 2 台`。此后在任一设备上复制文本，另一台立刻可以粘贴。

---

## 浮窗界面说明

```
┌─────────────────────────────────────┐
│ ● Sync Copy    小组 · 2 台  ⚙  ─  × │  ← 顶部状态栏
├─────────────────────────────────────┤
│                                     │
│  [历史条目 1]  本机 · 刚刚       ✕  │
│  [历史条目 2]  来自 工作Mac·3分前 ✕  │
│  [历史条目 3]  来自 Win-PC · 1小时前 │
│  ...                                │
│                                     │
├─────────────────────────────────────┤
│  📍 192.168.1.50:5858   设备名      │  ← 底部信息栏
└─────────────────────────────────────┘
```

### 状态点颜色含义

| 颜色 | 含义 |
|---|---|
| 绿色 | 已连接，小组内有其他设备在线 |
| 灰色 | 未连接（无其他在线设备）或仅本机运行 |
| 黄色 | 警告（如正在重连中） |
| 橙色 | 错误（如端口被占用、连接失败） |

### 顶部按钮

- **加入（蓝色胶囊按钮）**：在未加入任何小组时显示，点击弹出入组对话框
- **⚙（齿轮图标）**：打开设置面板
- **−（横线按钮）**：将浮窗收缩为悬浮球
- **×（关闭按钮）**：隐藏浮窗（不退出应用，仍在托盘运行）

---

## 历史列表用法

历史列表记录最近 50 条同步内容，按时间倒序排列。

### 文本条目

单击条目 → 文本写回系统剪切板，条目短暂闪烁并显示「已复制」。

### 图片条目

显示缩略图及尺寸标注。单击 → 图片写回系统剪切板，可直接在其他应用粘贴。

### 文件条目

显示 📎 文件名、大小和状态（「已保存」/「已发送」/「保存失败」）。单击 → 在 Finder（macOS）或资源管理器（Windows）中定位到文件。

### 删除条目

每条右侧有灰色 ✕ 按钮（悬停变红），点击删除该条目。

### 清空全部

进入设置面板 → 点击「清除全部历史」。

---

## 折叠为悬浮球

点击浮窗顶部「−」按钮，浮窗缩小为 48×48 的圆形悬浮球，停在屏幕角落。

- **单击悬浮球**：展开恢复完整浮窗
- **拖动悬浮球**：按住并移动超过 8 像素即为拖动，可移到屏幕任意位置

---

## 系统托盘菜单

右键点击系统托盘中的 Sync Copy 图标：

| 菜单项 | 功能 |
|---|---|
| 显示浮窗 | 重新显示浮窗并置前 |
| 隐藏浮窗 | 隐藏浮窗（应用仍在后台运行） |
| 退出 | 发出下线广播后退出应用 |

左键单击托盘图标可切换浮窗显示/隐藏。

---

## 设置面板

点击浮窗右上角 ⚙ 图标进入设置。

- **设备名**：修改本机在小组中显示的名称（最多 64 字符）。修改后切回主界面即生效，并持久化保存。
- **清除全部历史**：清除本机历史记录（同时广播给小组所有设备）。
- **退出应用**：向小组广播下线通知后退出。

---

## 跨平台说明

Sync Copy 同时支持 macOS 和 Windows，两平台功能完全对等。

- **macOS**：磨砂玻璃视觉效果完整（`backdrop-filter: blur`），浮窗圆角流畅
- **Windows**：视觉效果接近但使用半透明纯色背景（Mica 材质），功能无差异
- **混用**：Mac 和 Windows 可在同一小组正常互通，无需额外配置

---

## 安全说明

- 所有同步内容在 LAN 内**端到端加密**（X25519 ECDH 密钥协商 + AES-256-GCM 加密），LAN 内抓包只能看到密文
- 每次握手动态生成新密钥，进程退出即清除，不持久化到磁盘
- 新设备入组**必须由小组内任一在线设备手动点击「同意」**，不存在自动接受
- 信任关系仅在内存中保存，重启应用后需重新加入小组（设计如此，非缺陷）

---

## FAQ

**Q1：启动后对方看不到我，或者连接失败，怎么办？**

通常是防火墙拦截了 5858 端口。

- **macOS**：系统偏好设置 → 安全性与隐私 → 防火墙 → 允许 Sync Copy 接受入站连接
- **Windows**：运行以下命令（管理员模式）：

```
netsh advfirewall firewall add rule name="SyncCopy" dir=in action=allow protocol=TCP localport=5858
```

**Q2：我装了 Clash / Surge / VPN，会影响同步吗？**

不影响。Sync Copy 内置了 `.no_proxy()` 设置，所有 LAN 请求都绕过系统代理直连，无需关闭 Clash 或 VPN。

**Q3：A 和 B 在同一 WiFi 但一直连不上？**

可能是以下原因：

- 两台设备在不同网段（例如一台有线 192.168.0.x，另一台 WiFi 192.168.1.x）—— 需要同一网段
- 路由器开启了 AP 隔离（防止同 WiFi 设备互访）—— 需在路由器设置中关闭 AP 隔离
- 多张网卡干扰（VPN 虚拟网卡 / WSL 虚拟网卡）—— Sync Copy 会优先选 192.168/10/172.16 段实体网卡，但如有疑问可在设置中确认显示的 IP 是否是正确的 LAN IP

**Q4：历史记录最多能保存多少条？重启后还在吗？**

历史最多保存最近 50 条（超出后自动移除最旧的）。历史记录**仅保存在内存中，重启应用后清空**——这是有意的设计，避免敏感内容落盘。

配置（设备名、端口）会持久化保存，重启后恢复。

**Q5：文本太长同步不过去？**

文本同步上限为 1 MB。超过 1 MB 的文本不会被广播。如需传输大段文本，建议先存为文件再通过文件传输功能发送（文件上限 5 MB）。

**Q6：macOS 提示"Apple 无法验证 Sync Copy"或"已损坏，无法打开"怎么办？**

v2.0 未做 Apple 代码签名和公证（个人工具，参见 _assumptions A29），首次启动会被 Gatekeeper 拦截。三种放行方法，任选一种：

**方法 1：右键放行（最简单）**

1. 在 Finder 中右键（或 Control + 点击）`Sync Copy.app`
2. 选「打开」
3. 弹框再点「打开」
4. 之后双击启动即正常，无需重复操作

**方法 2：终端命令（适合"已损坏"提示）**

打开「终端」应用，输入以下命令（路径按 .app 实际位置调整）：

```
xattr -dr com.apple.quarantine /Applications/Sync\ Copy.app
```

执行后直接双击 .app 正常启动。

**方法 3：系统设置放行**

1. 系统设置 → 隐私与安全性
2. 滚动到「安全性」段
3. 看到「已阻止使用"Sync Copy"，因为它来自身份不明的开发者」→ 点「仍要打开」
4. 验证 Touch ID 或输入密码确认

三种方法放行后，后续双击启动均正常，不会再次弹出此提示。

---

## 已知限制

- **Linux 不在官方支持范围内**：理论上可以编译运行，但 Wayland 图片剪切板不稳定，CI 不产出 Linux 构建包
- **不支持富文本格式**：仅同步纯文本、PNG 图片、文件三类，不同步 RTF/HTML 格式
- **不支持移动端**：仅 macOS 和 Windows 桌面
- **重启后需重新加入小组**：信任关系不持久化，这是安全设计选择
- **不支持 mDNS 自动发现**：需要手动输入对方 IP:PORT
